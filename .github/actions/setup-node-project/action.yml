name: Setup Node Project
description: Setup Node from .nvmrc and install dependencies.

runs:
  using: composite
  steps:
    - name: Setup Node from .nvmrc
      uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: npm

    - name: Install dependencies
      run: npm ci
      shell: bash
